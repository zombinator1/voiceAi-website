---
import { myPhone, myPhoneHref, FIRMA } from '../config';
const rok = new Date().getFullYear();
---

<footer class="border-t border-linia bg-arkusz-glebszy py-10 sm:py-14">
  <div class="arkusz-kolumna">
    <div class="grid gap-8 sm:grid-cols-2 sm:gap-12">
      <div>
        <p class="font-display text-[1.0625rem] font-700 tracking-[-0.01em]">
          {FIRMA.nazwa}
        </p>
        <!-- Kolumna etykiet dopasowuje się do najdłuższej z nich (`auto`),
             więc wartości startują równo bez zgadywania szerokości.
             Poniżej `xs` etykieta ląduje nad wartością — „telefon osobisty"
             i adres e-mail nie mieszczą się obok siebie na wąskim telefonie. -->
        <dl class="mt-2 grid gap-x-3 xs:grid-cols-[auto_1fr] xs:items-center">
          <dt class="maszynowa text-notatka xs:whitespace-nowrap">e-mail</dt>
          <dd class="font-mono text-[0.875rem]">
            <a class="inline-flex min-h-11 items-center underline underline-offset-2 hover:text-sluchawka" href={`mailto:${FIRMA.email}`}>
              {FIRMA.email}
            </a>
          </dd>
          <dt class="maszynowa text-notatka xs:whitespace-nowrap">telefon osobisty</dt>
          <dd class="font-mono text-[0.875rem] tabular-nums">
            <a class="inline-flex min-h-11 items-center underline underline-offset-2 hover:text-sluchawka" href={`tel:${myPhoneHref}`}>
              {myPhone}
            </a>
          </dd>
        </dl>
      </div>

      <nav aria-label="Stopka" class="sm:justify-self-end">
        <ul class="space-y-1">
          <li>
            <a class="inline-flex min-h-11 items-center underline underline-offset-2 hover:text-sluchawka" href="/rodo">
              Polityka prywatności
            </a>
          </li>
          <li>
            <a class="inline-flex min-h-11 items-center underline underline-offset-2 hover:text-sluchawka" href="/#oferta">Cennik</a>
          </li>
          <li>
            <a class="inline-flex min-h-11 items-center underline underline-offset-2 hover:text-sluchawka" href="/#faq">
              Pytania i odpowiedzi
            </a>
          </li>
          <li>
            <button
              type="button"
              data-cookies-otworz
              class="inline-flex min-h-11 items-center underline underline-offset-2 hover:text-sluchawka"
            >
              Ustawienia cookies
            </button>
          </li>
        </ul>
      </nav>
    </div>

    <p class="maszynowa mt-10 border-t border-linia pt-6 text-notatka">
      © {rok} {FIRMA.nazwa}
    </p>
  </div>
</footer>
