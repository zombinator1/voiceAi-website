[build]
  command = "npm run build"
  publish = "dist"
  # Funkcja /api/kontakt — wysyłka formularza przez Lettermint.
  # Własną ścieżkę deklaruje sama funkcja (Netlify Functions v2).
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Frame-Options = "SAMEORIGIN"

[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
